<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.chalmers.dat255.group09.Alarmed"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/title_activity_browse_alarms" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.CreateAlarm"
            android:label="@string/title_activity_create_alarm" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.chalmers.dat255.group09.Alarmed.activity.CreateAlarm" />
        </activity>

        <receiver
            android:name=".receiver.AlarmReceiver"
            android:process=":remote" />

        <activity
            android:name=".activity.BaseActivationActivity"
            android:label="null" >
        </activity>
        <activity
            android:name=".modules.mathModule.activity.MathActivity"
            android:label="@string/title_activity_math"
            android:screenOrientation="portrait" >
        </activity>
    </application>

</manifest>